---
import Wrapper from './Wrapper.astro';

interface Props {
   className?: string;
}

const { className } = Astro.props;
---

<main class={className}>
   <Wrapper>
      <slot />
   </Wrapper>
</main>
